import React from 'react';
import { NavLink, Outlet } from 'react-router-dom';
import { LayoutDashboard, FileText, Users, Settings, HelpCircle, WalletCards, ShieldCheck, Building2, GitCompare, DatabaseBackup, ClipboardList, PlusSquare } from 'lucide-react';
import { useLiveQuery } from '@/src/db/db'; import { db } from '../db/db'; import { UserMenu } from './AuthGate'; import { canAccess, getSession } from '../services/auth';
const Layout=()=>{const settings=useLiveQuery(()=>db.settings.get(1));const agencyName=settings?.agencyName||'سامانه املاک';const session=getSession();return <div className="flex h-screen bg-slate-50 text-slate-900 font-sans overflow-hidden" dir="rtl"><aside className="hidden md:flex flex-col w-64 bg-slate-900 text-white shadow-lg z-20"><div className="px-6 py-4 flex items-center gap-3 border-b border-slate-800"><div className="w-10 h-10 bg-emerald-500 rounded-lg flex items-center justify-center font-bold text-xl">{agencyName.charAt(0)}</div><div><h1 className="text-lg font-bold truncate w-40">{agencyName}</h1><p className="text-[10px] text-slate-400 mt-1">سامانه جامع مشاور املاک</p></div></div><nav className="flex-1 flex flex-col py-4 gap-1 px-3"><NavItem to="/" icon={<LayoutDashboard size={20}/>} label="داشبورد"/>{session&&canAccess(session.role,'properties')&&<><NavItem to="/property-new" icon={<PlusSquare size={20}/>} label="ثبت ملک"/><NavItem to="/properties" icon={<Building2 size={20}/>} label="فایل‌های املاک"/><NavItem to="/property-requests" icon={<ClipboardList size={20}/>} label="ثبت تقاضا"/><NavItem to="/matching" icon={<GitCompare size={20}/>} label="مچ ملک و تقاضا"/></>}{session&&canAccess(session.role,'contracts')&&<NavItem to="/contracts" icon={<FileText size={20}/>} label="قرارداد"/>}{session&&canAccess(session.role,'finance')&&<NavItem to="/finance" icon={<WalletCards size={20}/>} label="مدیریت مالی"/>}{session&&canAccess(session.role,'customers')&&<NavItem to="/customers" icon={<Users size={20}/>} label="مشتریان"/>}{canAccess(session?.role||'agent','users')&&<NavLink to="/users" className={({isActive})=>`flex items-center gap-3 px-4 py-3 rounded-xl ${isActive?'bg-emerald-600 text-white font-bold':'text-slate-400 hover:bg-slate-800'}`}><ShieldCheck size={20}/>مدیریت کاربران</NavLink>}{session&&canAccess(session.role,'settings')&&<NavItem to="/settings" icon={<Settings size={20}/>} label="تنظیمات"/>}{session&&canAccess(session.role,'settings')&&<NavItem to="/backup" icon={<DatabaseBackup size={20}/>} label="پشتیبان‌گیری"/>}<NavItem to="/help" icon={<HelpCircle size={20}/>} label="راهنما"/></nav><div className="p-4 border-t border-slate-800 text-[11px] text-slate-400 flex flex-col gap-3"><UserMenu/><span>POS: متصل</span><span>چاپگر: آماده</span></div></aside><div className="flex flex-col flex-1 overflow-hidden"><header className="md:hidden bg-slate-900 text-white px-4 py-3 flex items-center justify-between"><div className="flex items-center gap-3"><div className="w-8 h-8 bg-emerald-500 rounded-lg flex items-center justify-center font-bold">{agencyName.charAt(0)}</div><h1 className="text-lg font-bold truncate max-w-[200px]">{agencyName}</h1></div><UserMenu/></header><main className="flex-1 overflow-y-auto p-4 pb-20 md:pb-4"><div className="max-w-7xl mx-auto h-full"><Outlet/></div></main><nav className="md:hidden fixed bottom-0 w-full bg-slate-900 flex justify-around z-20"><NavItem to="/" icon={<LayoutDashboard size={24}/>} label="خانه"/>{session&&canAccess(session.role,'properties')&&<NavItem to="/property-new" icon={<PlusSquare size={24}/>} label="ثبت ملک"/>}{session&&canAccess(session.role,'properties')&&<NavItem to="/property-requests" icon={<ClipboardList size={24}/>} label="تقاضا"/>}{session&&canAccess(session.role,'customers')&&<NavItem to="/customers" icon={<Users size={24}/>} label="مشتریان"/>}</nav></div></div>};
const NavItem=({to,icon,label}:{to:string;icon:React.ReactNode;label:string})=><NavLink to={to} className={({isActive})=>`flex md:flex-row flex-col items-center justify-center md:justify-start w-full py-2 md:py-3 md:px-4 md:rounded-lg ${isActive?'text-emerald-400 font-bold md:bg-emerald-600 md:text-white':'text-slate-400 hover:text-slate-200 md:hover:bg-slate-800'} transition-colors`}>{icon}<span className="text-xs md:text-sm mt-1 md:mt-0 md:mr-3 font-medium">{label}</span></NavLink>;
export default Layout;
